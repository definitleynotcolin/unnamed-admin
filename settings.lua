-- module script

return {
	-- admin settings --
	admins = {game.CreatorId},
	adminbffs = false,
	adminfriends = false,
	-- banned settings --
	banned = {},
	autokick = true,
	-- command settings --
	runcommand = "!", -- starting of command. ex: !kill
	divider = " ", -- dividing command and arguments. ex: !kill player
	viewcommanddocumentation = "?",
	-- do not mess with below --
	openbracket = "[",
	closedbracket = "]"
}
